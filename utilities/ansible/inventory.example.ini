# Copy this file to inventory.ini and fill in your target server(s).
# inventory.ini is gitignored so credentials/hostnames don't get committed.

[lsst_servers]
linux-physik ansible_host=HOSTNAME ansible_user=USER
